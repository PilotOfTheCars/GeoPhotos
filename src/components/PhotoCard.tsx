import Link from "next/link";import {Photo} from "@/lib/data";export default function PhotoCard({p,large=false}:{p:Photo,large?:boolean}){return <Link href={"/photos/"+p.id} className={"photoCard "+(large?"large":"")}><div className="photoArt"><span>{p.id}</span></div><div className="photoInfo"><strong>{p.title}</strong><span>{p.aircraft} · {p.icao}<br/>by {p.photographer}</span></div></Link>}