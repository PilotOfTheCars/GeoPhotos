import SiteHeader from "@/components/SiteHeader";import PhotoCard from "@/components/PhotoCard";import {photos} from "@/lib/data";export default function Explore(){return <><SiteHeader/><main className="shell page"><div className="kicker">ARCHIVE</div><h1>Explore GeoFS</h1><div className="filters"><button>Latest</button><button>Awarded</button><button>Random</button><button>Aircraft</button><button>Airport</button><button>Capture type</button></div><div className="archiveGrid">{photos.map(p=><PhotoCard key={p.id} p={p}/>)}</div></main></>}