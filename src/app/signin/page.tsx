import SiteHeader from "@/components/SiteHeader";import {signIn} from "@/lib/auth";export default function SignIn(){return <><SiteHeader/><main className="shell page narrow"><div className="kicker">ACCOUNT</div><h1>Sign in.</h1><p className="lede">Your GeoPhotos account is your portfolio, submissions and QC history.</p><div className="authStack"><form action={async()=>{"use server";await signIn("discord",{redirectTo:"/account"})}}><button className="primary">Continue with Discord</button></form><form action={async()=>{"use server";await signIn("google",{redirectTo:"/account"})}}><button className="secondary">Continue with Google</button></form><p className="fine">Only providers configured by the server will work.</p></div></main></>}