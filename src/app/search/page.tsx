"use client";import {useState} from "react";import SiteHeader from "@/components/SiteHeader";import PhotoCard from "@/components/PhotoCard";import {photos} from "@/lib/data";export default function Search(){const[q,setQ]=useState("");const r=photos.filter(p=>Object.values(p).join(" ").toLowerCase().includes(q.toLowerCase()));return <><SiteHeader/><main className="shell page"><div className="kicker">SEARCH</div><h1>Find a photograph.</h1><input className="searchBox" value={q} onChange={e=>setQ(e.target.value)} placeholder="Aircraft, ICAO, photographer, title…"/><div className="archiveGrid">{r.map(p=><PhotoCard key={p.id} p={p}/>)}</div></main></>}