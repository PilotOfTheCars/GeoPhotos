import {notFound} from "next/navigation";import SiteHeader from "@/components/SiteHeader";import {photos} from "@/lib/data";export default async function PhotoPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const p=photos.find(x=>x.id===id);if(!p)notFound();return <><SiteHeader/><main className="shell detail"><div className="detailArt"><span>{p.id}</span></div><div className="detailMeta"><div className="kicker">{p.award||"ARCHIVE PHOTOGRAPH"}</div><h1>{p.title}</h1><dl><dt>Aircraft</dt><dd>{p.aircraft}</dd><dt>Location</dt><dd>{p.airport} · {p.icao}</dd><dt>Photographer</dt><dd>{p.photographer}</dd><dt>Capture</dt><dd>{p.capture}</dd><dt>Permanent ID</dt><dd>{p.id}</dd></dl></div></main></>}