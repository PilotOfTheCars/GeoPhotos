"use client";import {useState} from "react";import SiteHeader from "@/components/SiteHeader";export default function Submit(){const[done,setDone]=useState(false);return <><SiteHeader/><main className="shell page narrow"><div className="kicker">SUBMISSION</div><h1>Publish to GeoPhotos.</h1>{done?<div className="notice"><b>Submission saved.</b><p>This local MVP has recorded the workflow state in your browser. Production storage and staff QC will connect to the database next.</p></div>:<form className="form" onSubmit={e=>{e.preventDefault();setDone(true)}}><label>Image<input type="file" accept="image/*" required/></label><label>Title<input required placeholder="Morning Departure"/></label><div className="two"><label>Aircraft<input required placeholder="Airbus A350-900"/></label><label>Airport / ICAO<input placeholder="WSSS"/></label></div><label>Capture type<select><option>Spotted</option><option>Self-shot</option><option>Staged</option></select></label><label>Description<textarea rows={5} placeholder="Optional context about the frame."/></label><button className="primary">Submit for QC</button><p className="fine">GP numbers are assigned only after QC acceptance.</p></form>}</main></>}