import {redirect} from "next/navigation";import SiteHeader from "@/components/SiteHeader";import {auth} from "@/lib/auth";import {hasPermission} from "@/lib/permissions";import QCQueue from "@/components/QCQueue";export default async function QC(){const s=await auth(),id=(s?.user as any)?.id;if(!id)redirect("/signin");if(!(await hasPermission(id,"qc.review")))redirect("/account");return <><SiteHeader/><main className="shell page"><div className="kicker">STAFF · QUALITY CONTROL</div><h1>Review queue.</h1><QCQueue/></main></>}