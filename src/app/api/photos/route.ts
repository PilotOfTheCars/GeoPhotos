import {NextRequest,NextResponse} from "next/server";import {db} from "@/lib/db";export async function GET(req:NextRequest){const q=req.nextUrl.searchParams.get("q")?.trim();const take=Math.min(Number(req.nextUrl.searchParams.get("limit")||24),60);const photos=await db.photo.findMany({where:q?{OR:[{title:{contains:q,mode:"insensitive"}},{gpId:{contains:q,mode:"insensitive"}},{photographer:{username:{contains:q,mode:"insensitive"}}},{aircraft:{name:{contains:q,mode:"insensitive"}}},{airport:{icao:{contains:q,mode:"insensitive"}}}]}:undefined,include:{photographer:{select:{username:true,displayName:true}},aircraft:true,airport:true,operator:true,category:true},orderBy:{acceptedAt:"desc"},take});return NextResponse.json({photos})}