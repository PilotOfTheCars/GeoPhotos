import {NextRequest,NextResponse} from "next/server";import {z} from "zod";import {auth} from "@/lib/auth";import {db} from "@/lib/db";const Input=z.object({title:z.string().trim().min(2).max(100),description:z.string().trim().max(2000).optional(),imageKey:z.string().trim().min(1).max(500),captureType:z.enum(["SPOTTED","SELF_SHOT","STAGED"]),aircraftId:z.string().cuid().optional(),airportId:z.string().cuid().optional(),operatorId:z.string().cuid().optional(),categoryId:z.string().cuid().optional()});export async function POST(req:NextRequest){const session=await auth(),userId=(session?.user as any)?.id;if(!userId)return NextResponse.json({error:"Unauthorised"},{status:401});const parsed=Input.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:"Invalid submission",issues:parsed.error.flatten()},{status:400});const submission=await db.photoSubmission.create({data:{...parsed.data,ownerId:userId,status:"SUBMITTED"}});return NextResponse.json({submission},{status:201})}export async function GET(){const session=await auth(),userId=(session?.user as any)?.id;if(!userId)return NextResponse.json({error:"Unauthorised"},{status:401});return NextResponse.json({submissions:await db.photoSubmission.findMany({where:{ownerId:userId},include:{reviews:true},orderBy:{createdAt:"desc"}})})}