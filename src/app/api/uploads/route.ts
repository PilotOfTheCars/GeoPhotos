import {NextRequest,NextResponse} from "next/server";import {auth} from "@/lib/auth";import {storeImage} from "@/lib/storage";export async function POST(req:NextRequest){const session=await auth();if(!(session?.user as any)?.id)return NextResponse.json({error:"Unauthorised"},{status:401});try{const form=await req.formData(),file=form.get("file");if(!(file instanceof File))return NextResponse.json({error:"Image required"},{status:400});const imageKey=await storeImage(file);return NextResponse.json({imageKey},{status:201})}catch(e){const m=e instanceof Error?e.message:"UPLOAD_FAILED";const status=m==="SIZE"?413:m==="TYPE"?415:m==="STORAGE_NOT_CONFIGURED"?503:500;return NextResponse.json({error:m},{status})}}