import SiteHeader from "@/components/SiteHeader";import PhotoCard from "@/components/PhotoCard";import {photos} from "@/lib/data";export default async function Profile({params}:{params:Promise<{name:string}>}){const{name}=await params;const n=decodeURIComponent(name),mine=photos.filter(p=>p.photographer===n);return <><SiteHeader/><main className="shell page"><div className="profileHead"><div className="avatar big">{n[0]}</div><div><div className="kicker">PHOTOGRAPHER</div><h1>{n}</h1><p>{mine.length} published photograph{mine.length===1?"":"s"}</p></div></div><div className="archiveGrid">{mine.map(p=><PhotoCard key={p.id} p={p}/>)}</div></main></>}