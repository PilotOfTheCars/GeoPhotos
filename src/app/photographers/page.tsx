import SiteHeader from "@/components/SiteHeader";import Link from "next/link";import {db} from "@/lib/db";export const dynamic="force-dynamic";export default async function Photographers(){let people:any[]=[];try{people=await db.user.findMany({where:{photos:{some:{}}},include:{_count:{select:{photos:true}}},orderBy:{photos:{_count:"desc"}},take:100})}catch{}return <><SiteHeader/><main className="shell page"><div className="kicker">DIRECTORY</div><h1>Photographers</h1><div className="people">{people.map(p=><Link href={"/photographers/"+p.username} className="person" key={p.id}><div className="avatar">{p.displayName[0]}</div><b>{p.displayName}</b><span>@{p.username} · {p._count.photos} published</span></Link>)}</div></main></>}