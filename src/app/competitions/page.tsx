import SiteHeader from "@/components/SiteHeader";import Link from "next/link";import {competitions} from "@/lib/data";export default function Competitions(){return <><SiteHeader/><main className="shell page"><div className="kicker">COMPETITIONS</div><h1>Make the frame count.</h1><p className="lede">Editorial challenges for GeoFS photographers. Accepted archive photographs can compete.</p><div className="list">{competitions.map((c,i)=><Link href={"/competitions/"+c.slug} className="listRow" key={c.slug}><span>0{i+1}</span><div><b>{c.name}</b><p>{c.copy}</p></div><em>{c.status}</em></Link>)}</div></main></>}