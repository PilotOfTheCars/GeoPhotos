import {notFound} from "next/navigation";import SiteHeader from "@/components/SiteHeader";import {competitions} from "@/lib/data";export default async function Competition({params}:{params:Promise<{slug:string}>}){const{slug}=await params,c=competitions.find(x=>x.slug===slug);if(!c)notFound();return <><SiteHeader/><main className="shell page narrow"><div className="kicker">COMPETITION · {c.status}</div><h1>{c.name}</h1><p className="lede">{c.copy}</p><div className="prose"><h2>Brief</h2><p>Submit a photograph that interprets the theme through composition, timing and aviation subject matter. The photograph must first meet normal GeoPhotos QC standards.</p><h2>Eligibility</h2><p>Spotted, self-shot and staged capture types are eligible unless a future competition brief says otherwise. Voting and final selection are separate from archive QC.</p></div></main></>}