import Link from "next/link";import SiteHeader from "@/components/SiteHeader";import PhotoCard from "@/components/PhotoCard";import {photos,competitions} from "@/lib/data";export default function Home(){return <><SiteHeader/><main><section className="hero"><div className="shell heroInner"><div><div className="kicker">PHOTO OF THE DAY · {photos[0].id}</div><h1>{photos[0].title}</h1><p>{photos[0].aircraft} · {photos[0].airport} — {photos[0].icao}<br/>Photograph by {photos[0].photographer}</p></div><Link href={"/photos/"+photos[0].id}>View photograph ↗</Link></div></section><section className="shell section"><div className="sectionHead"><div><div className="kicker">ARCHIVE</div><h2>Latest Photography</h2></div><Link href="/explore">Explore all →</Link></div><div className="homeGrid">{photos.slice(1).map((p,i)=><PhotoCard key={p.id} p={p} large={i===0}/>)}</div></section><section className="shell split"><div><div className="kicker">CURRENT COMPETITION</div><h2>{competitions[0].name}</h2><p>{competitions[0].copy}</p><Link className="textLink" href={"/competitions/"+competitions[0].slug}>View competition →</Link></div><div className="stat"><strong>000004</strong><span>PHOTOGRAPHS IN THE ARCHIVE</span></div></section></main></>}